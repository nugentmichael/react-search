import React from 'react';

const Footer = () => {
	return (
		<footer className="App-footer">
			Roogle: A React Search App &copy; {new Date().getFullYear()}
		</footer>
	);
};

export default Footer;
